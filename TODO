* Add watchers on layout files to remove cache.
* Rework the in memory game data to be updateable from the game_scraper.
* Implement other pages that are present on static version of site.
* cache the controller, and recalculate every hour or so.
